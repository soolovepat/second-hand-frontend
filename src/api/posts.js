import api from "./axios";

export const writePost = (req) => {};

export const getPosts = (req) => {};

export const getUserPosts = (req) => {};

export const getUserPost = (req) => {};

export const editPost = (req) => {};

export const deletePost = (req) => {};
